version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd aiedulog
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: aiedulog/.next
    files:
      - '**/*'
  cache:
    paths:
      - aiedulog/node_modules/**/*